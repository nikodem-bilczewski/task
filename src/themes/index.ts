import { createTheme } from '@mui/material'

export const smallFontTheme = createTheme({
  typography: {
    fontSize: 18,
  },
})

export const bigFontTheme = createTheme({
  typography: {
    fontSize: 36,
  },
})
